<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.changzheng.activity_left.MainActivity">

    <Button
        android:id="@+id/setImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#00000000"
        android:textColor="#33c67f"
        android:onClick="changeImage"
        android:text="设置头像" />
    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_below="@id/setImage"
        android:layout_marginTop="10dp"
        android:id="@+id/head_iv"/>

    <Button
        android:id="@+id/otheraty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#00000000"
        android:textColor="#33c67f"
        android:layout_below="@id/head_iv"
        android:onClick="otheraty"
        android:text="进入另外一个aty" />

</RelativeLayout>
